# Brief for Varro — Find/Create a home for job hunting future ideas

**From:** Adama
**Date:** 2026-04-03

---

## Task

We need a place to capture future development ideas related to the job hunting portfolio (Projects 1–4). These are not immediate work items — they are "consider for later" notes that shouldn't clutter the active project plans.

## What to do

1. Look at the existing job hunting project structure (`Projects/` directory, existing `brief.md` and `plan.md` files) and assess whether there's already a natural home for a roadmap/wishlist
2. If not, create one — either a single `Projects/Job Hunting Portfolio/roadmap.md` covering all four projects, or a conventions-based section in each project's plan
3. Recommend and implement whichever approach feels most coherent with the existing structure

## First item to add

Once the location is established, add the following idea:

**UI for managing interesting_companies table**
- Currently companies are added manually via SQL as a one-off
- A simple UI — add, edit, view entries in `interesting_companies` — would make this accessible without needing CLI/DB access
- The table has rich fields (interest drivers, apprehensions, size, sector, culture, evilness, etc.) that are currently unused because there's no way to populate them easily
- Consider for future Iris work

---

## Deliverable

Tell Adama in chat where future ideas should go, and confirm the first item has been added.
